## 1. Design-system foundation

- [ ] 1.1 Create a committed Shadcn official component inventory for `packages/client` and use it as the source list for materialization; done when the inventory covers every official Shadcn UI component entry and is referenced by verification tests for `[CLIENT-DESIGN-SYSTEM-S001]`.
- [ ] 1.2 From `packages/client`, run `pnpm dlx shadcn@latest add <official-component-names> --overwrite` against the committed inventory and materialize every component into `packages/client/src/components/ui/**`; done when existing and added components are editable local TSX source and no runtime Shadcn registry/component package consumption remains. `[CLIENT-DESIGN-SYSTEM-S001]`
- [ ] 1.3 Resolve Shadcn component dependencies through normal pnpm workflow without weakening `minimumReleaseAge: 4320`, without wildcard `allowBuilds`, and without `dangerouslyAllowAllBuilds`; done when `pnpm lint:supply-chain` passes after dependency resolution.
- [ ] 1.4 Consolidate `cn` usage on `packages/client/src/lib/utils.ts` and update local Shadcn imports away from `packages/client/src/components/ui/cn.ts`; done when the duplicate helper file is gone and all UI components compile. `[CLIENT-DESIGN-SYSTEM-S001]`
- [ ] 1.5 Replace `packages/client/app/globals.css` with Shadcn default neutral token blocks, Tailwind directives, and required base layer only; done when product-specific global palette variables, route visual shim classes, bespoke gradients, custom font overrides, and table-only responsive CSS shims are no longer styling sources. `[CLIENT-DESIGN-SYSTEM-S002]`
- [ ] 1.6 Update `packages/client/tailwind.config.ts` to use Shadcn default semantic token mapping and repository content paths; done when independent color/font/radius aliases outside Shadcn default token strategy are not used. `[CLIENT-DESIGN-SYSTEM-S002]`
- [ ] 1.7 Update reusable UI composition components to consume local Shadcn primitives and browser-safe props only; done when `packages/client/src/components/**` does not import server-only Agent RPC modules, Connect runtime construction, generated Agent RPC clients, credential resolvers, or Agent runtime source. `[CLIENT-DESIGN-SYSTEM-S004]`

## 2. Management shell and navigation

- [ ] 2.1 Introduce the Management Client left-side shell in `packages/client/app/layout.tsx` or a management route layout; done when desktop renders persistent left navigation and narrow viewports render accessible Sheet navigation with skip link and focus return. `[MANAGEMENT-CLIENT-SHELL-S009]` `[CLIENT-DESIGN-SYSTEM-S003]`
- [ ] 2.2 Implement global navigation with only `Agents` and `Global Settings`; done when `/agents` renders the supported global entry and no Agent identity is required to reach global screens. `[MANAGEMENT-CLIENT-SHELL-S001]` `[MANAGEMENT-CLIENT-SHELL-S009]`
- [ ] 2.3 Add `/global-settings` as a Client-wide settings screen; done when it renders Client-wide controls only and does not render selected-Agent identity or Agent scoped actions. `[MANAGEMENT-CLIENT-SHELL-S013]`
- [ ] 2.4 Implement selected-Agent navigation with `Overview`, `Threads`, `Events`, `Runs`, `Schedules`, `Integrations`, and `Settings`; done when selecting an Agent displays this menu with Agent identity while preserving global navigation access. `[MANAGEMENT-CLIENT-SHELL-S010]`
- [ ] 2.5 Make `New Agent` a primary action on `Agents` screen that opens the registration flow; done when the form is reached from the Agents entry action and remains accessible by keyboard and screen reader. `[MANAGEMENT-CLIENT-SHELL-S011]` `[AGENT-MANAGEMENT-UI-S002]`
- [ ] 2.6 Move ToolInvocation, Tool catalog, ThreadCompaction, Handoff, History reference, and Memory version views into the relevant Runs, Events, Settings, Threads, and Overview detail regions; done when the supported selected-Agent screens expose these contextual details with selected Agent scope visible. `[MANAGEMENT-CLIENT-SHELL-S012]` `[AGENT-MANAGEMENT-UI-S020]`
- [ ] 2.7 Update route graph implementation to the supported management shell routes `/agents`, `/global-settings`, `/agents/new`, `/agents/[agentId]`, `threads`, `events`, `runs`, `schedules`, `integrations`, and `settings`; done when route enumeration tests identify this supported graph. `[MANAGEMENT-CLIENT-SHELL-S007]`

## 3. Agents entry and selected-Agent screens

- [ ] 3.1 Rebuild `packages/client/app/agents/page.tsx` and related components as card/list registry with pin, sort order, last-opened metadata, credential status, and selection action; done when selection updates last-opened metadata via server-side action. `[AGENT-MANAGEMENT-UI-S001]`
- [ ] 3.2 Rebuild Agent registration form with Shadcn `Form`, field messages, server-side validation, credential reference input, RPC origin validation, and initial model policy fields; done when invalid input is announced through associated field errors and initial model policy is submitted server-side with the Agent registration flow. `[AGENT-MANAGEMENT-UI-S002]` `[AGENT-MANAGEMENT-UI-S017]`
- [ ] 3.3 Rebuild selected-Agent Overview as card/list/detail composition for profile, lifecycle, config version, credential generation, capability summary, and latest Memory/Compaction summary; done when browser payload stays secret-free. `[AGENT-MANAGEMENT-UI-S003]` `[AGENT-MANAGEMENT-UI-S019]`
- [ ] 3.4 Rebuild selected-Agent Settings with configuration, model policy, credentials, and Tool catalog sections; done when config update, credential rotation, and model policy update results render updated versions or secret-free errors. `[AGENT-MANAGEMENT-UI-S004]` `[AGENT-MANAGEMENT-UI-S018]` `[AGENT-MANAGEMENT-UI-S020]`
- [ ] 3.5 Rebuild Threads, Events, and Runs sections with list/detail composition, sequence/status/causal links/provenance, and scoped filtering; done when Agent/Thread scope is visible and preserved across paging/filtering. `[AGENT-MANAGEMENT-UI-S005]` `[AGENT-MANAGEMENT-UI-S019]`
- [ ] 3.6 Rebuild Schedule section with create, inspect, cancel, next fire, overlap policy, and action result states; done when server-side `CreateSchedule` and `CancelSchedule` flows update the UI through secret-free action states. `[AGENT-MANAGEMENT-UI-S006]`
- [ ] 3.7 Rebuild Tool approval context in Runs, Events, and Settings using explicit confirmation, risk metadata, acting user context, and approval/reject states; done when approval actions are disabled during pending state and require user confirmation. `[AGENT-MANAGEMENT-UI-S007]` `[AGENT-MANAGEMENT-UI-S020]`
- [ ] 3.8 Rebuild Integration list/detail screen with installation state, manifest identity, provider identity, grants, Adapter Connection, Tool, Delivery capability, setup instructions, and cleanup result; done when install/uninstall remains server-side Agent RPC only. `[AGENT-MANAGEMENT-UI-S008]` `[AGENT-MANAGEMENT-UI-S019]`
- [ ] 3.9 Implement responsive detail behavior for selected-Agent screens with Sheet/Dialog/Drawer local Shadcn components; done when narrow viewport detail surfaces provide focus trap, Escape close, focus return, and selected Agent scope labeling. `[AGENT-MANAGEMENT-UI-S021]` `[CLIENT-DESIGN-SYSTEM-S003]`

## 4. Automated tests and scenario traceability

- [ ] 4.1 Update Client shell tests with bracketed title `[MANAGEMENT-CLIENT-SHELL-S001] Agent registry shell が sidebar shell と Agents entry を描画する`; done when `/agents` renders supported registry shell, global nav, registration action, and detail affordances.
- [ ] 4.2 Update browser secrecy tests with bracketed title `[MANAGEMENT-CLIENT-SHELL-S002] Browser bundle が Agent RPC seam を含まない`; done when browser chunks exclude Agent credential material, direct Agent RPC invocation logic, Agent runtime imports, and Agent RPC client construction.
- [ ] 4.3 Update route graph tests with bracketed title `[MANAGEMENT-CLIENT-SHELL-S007] Management route graph が supported Agent sections を公開する`; done when the supported management route graph is detected without making the test purpose a previous-surface absence check.
- [ ] 4.4 Add navigation state test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S009] Agent 未選択時の左サイドメニューが global scope を表示する`; done when Agent-unselected shell shows global navigation and hidden/disabled selected-Agent semantics.
- [ ] 4.5 Add selected-Agent navigation test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S010] Agent 選択後に selected-Agent navigation が表示される`; done when selected-Agent navigation labels and Agent identity render with global navigation access.
- [ ] 4.6 Add registration action test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S011] New Agent action が Agents screen から registration flow を開く`; done when Agents screen action opens accessible registration flow.
- [ ] 4.7 Add contextual detail navigation test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S012] Tool と Compaction context が選択中 Agent 画面内で確認できる`; done when Tool and Compaction information is verified through Runs/Events/Settings/Threads/Overview detail regions.
- [ ] 4.8 Add Global Settings scope test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S013] Global Settings が Client-wide 設定だけを表示する`; done when Client-wide controls render without selected-Agent context.
- [ ] 4.9 Add design-system inventory test with bracketed title `[CLIENT-DESIGN-SYSTEM-S001] 全 Shadcn 公式 component が local source として存在する`; done when official inventory entries all map to local editable source under `packages/client/src/components/ui/**`.
- [ ] 4.10 Add styling boundary test with bracketed title `[CLIENT-DESIGN-SYSTEM-S002] Shadcn default token だけが styling source である`; done when globals, Tailwind config, and component source use only Shadcn default token strategy and local utilities.
- [ ] 4.11 Add responsive/reduced-motion component test with bracketed title `[CLIENT-DESIGN-SYSTEM-S003] responsive shell と reduced motion state が動作する`; done when sidebar/detail surfaces and loading indicators respect viewport and motion preferences.
- [ ] 4.12 Add UI import-boundary test with bracketed title `[CLIENT-DESIGN-SYSTEM-S004] UI component 層が Agent RPC seam を import しない`; done when component/app browser-visible modules exclude Agent RPC, Connect construction, credential resolver, generated Agent RPC construction, and Agent runtime imports.
- [ ] 4.13 Update Agent list test with bracketed title `[AGENT-MANAGEMENT-UI-S001] Agent list が card list と並び順を支援する`; done when card/list registry preserves pin order, sort order, and selection action.
- [ ] 4.14 Update Add Agent validation test with bracketed title `[AGENT-MANAGEMENT-UI-S002] Add Agent form が connection metadata を検証する`; done when accessible field errors are associated and server validation gates creation.
- [ ] 4.15 Update Overview rendering test with bracketed title `[AGENT-MANAGEMENT-UI-S003] Overview が profile と config を secret-free に描画する`; done when profile, lifecycle, config, credential generation, capability, and Memory/Compaction summary render without secrets.
- [ ] 4.16 Update Settings action test with bracketed title `[AGENT-MANAGEMENT-UI-S004] Settings が config update と credential rotation の結果を表示する`; done when action result states update config version or credential generation with secret-free errors.
- [ ] 4.17 Update exploration view test with bracketed title `[AGENT-MANAGEMENT-UI-S005] Thread Event Run と Compaction sections が Agent-owned history を表示する`; done when sequence, status, causal link, provenance, paging, and filtering preserve scope.
- [ ] 4.18 Update Schedule action test with bracketed title `[AGENT-MANAGEMENT-UI-S006] Schedule section が schedules を作成し cancel する`; done when create/cancel flows render status, next fire, overlap policy, and cancel result.
- [ ] 4.19 Update Tool approval test with bracketed title `[AGENT-MANAGEMENT-UI-S007] Tool 承認 context が明示 action を要求する`; done when approve/reject controls show metadata and require confirmation.
- [ ] 4.20 Update Integration screen test with bracketed title `[AGENT-MANAGEMENT-UI-S008] Integration 画面が汎用 Integration を install、list、uninstall する`; done when card/list/detail installation state, grants, setup, and cleanup result render.
- [ ] 4.21 Add card/list/detail IA test with bracketed title `[AGENT-MANAGEMENT-UI-S019] selected-Agent screens が card list detail 構成で表示される`; done when selected-Agent screens expose summary, filters, list, and detail regions.
- [ ] 4.22 Add contextual Tool/Compaction test with bracketed title `[AGENT-MANAGEMENT-UI-S020] Tool と Compaction が文脈情報として表示される`; done when ToolInvocation and ThreadCompaction details render in relevant Agent scoped contexts.
- [ ] 4.23 Add responsive detail test with bracketed title `[AGENT-MANAGEMENT-UI-S021] モバイル幅で selected-Agent detail が Sheet と focus management を使う`; done when mobile detail surfaces preserve focus behavior and Agent scope label.
- [ ] 4.24 Update existing `[AGENT-MANAGEMENT-UI-S009]` browser credential secrecy tests if UI routes or components move; done when the test still covers Agent list, Overview, Threads, Events, Runs, Schedules, Integrations, Settings, and Global Settings navigation without credential exposure.
- [ ] 4.25 Add no-proxy security boundary test with bracketed title `[MANAGEMENT-CLIENT-SHELL-S008] Client exposes no Agent API proxy routes`; done when route handlers, route manifest, and browser-visible network behavior show only supported management pages and internal Server Action boundaries, without using a previous-route absence check as the test purpose.
- [ ] 4.26 Update Agent registration model policy test with bracketed title `[AGENT-MANAGEMENT-UI-S017] Agent registration flow が initial model policy を server-side で送信する`; done when registration action validates model policy server-side, sends initial model policy and `initialConfig.modelPolicyRef`, and returns no Agent or Provider credential material to the browser.
- [ ] 4.27 Update Settings model policy test with bracketed title `[AGENT-MANAGEMENT-UI-S018] Settings 画面が default model policy を安全に更新する`; done when selected-Agent Settings updates policy ref, digest, provider, model, and config version through server-side Agent RPC and renders secret-free validation/permission errors.

## 5. Verification and release gates

- [ ] 5.1 Run `openspec validate --type change redesign-management-client-ui-shadcn --strict --no-interactive`; done when the change validates successfully.
- [ ] 5.2 Run `pnpm --filter @cf-tamac/client check`; done when Client type checks pass.
- [ ] 5.3 Run `pnpm test:client`; done when Client tests including all bracketed Scenario ID titles pass.
- [ ] 5.4 Run `pnpm lint`; done when ESLint, OpenSpec strict validation, Scenario coverage, governance, and supply-chain checks pass without bypass.
- [ ] 5.5 Run `pnpm build:client`; done when the Management Client builds successfully for Cloudflare/OpenNext target.
- [ ] 5.6 Run `pnpm check:codegen`; done when Agent TypeSpec/proto/generated RPC outputs have no drift.
- [ ] 5.7 Run `git diff --exit-code -- packages/agent/src/typespec packages/agent/proto packages/agent/src/generated/rpc packages/client/src/generated/agent-rpc`; done when this UI redesign has no Agent contract or generated output diff.
- [ ] 5.8 Run Impeccable and design-audit gates for changed presentation paths, including `node .opencode/skills/impeccable/scripts/detect.mjs packages/client/app packages/client/src/components`; done when evidence is recorded and violations are resolved.
- [ ] 5.9 Review browser-visible import graph after Shadcn materialization; done when no component/app import introduces `server-only`, Agent RPC factory, Connect runtime construction, generated Agent RPC construction, credential resolver, Agent runtime source, or direct Agent fetch.
