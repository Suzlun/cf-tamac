/**
 * Test stub for the `server-only` package.
 *
 * In production, `server-only` throws when imported by browser bundles. In the
 * Node.js test environment, it is a no-op so server modules can be exercised.
 */
export {};
